using Microsoft.AspNetCore.SignalR;

namespace CarStockBLL.Hubs
{
    /// <summary>
    /// Класс обработки запросов и ретрансляции подключенным клиентам
    /// </summary>
    public class NotifierHub : Hub
    {
        // Сервер сам отправляет сообщения
    }
}
